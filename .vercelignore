# Keep the serverless function lean — exclude everything the upstash backend
# doesn't need at runtime. (Sample docs are pre-ingested into Upstash, not read
# by the function; the frontend deploys as its own Vercel project.)
frontend/
.venv/
__pycache__/
chroma_db/
docs/
Dockerfile
docker-compose.yml
requirements-full.txt
benchmark.py
evaluate.py
migrate.py
ARCHITECTURE.md
