# Askable — SLIM deps for the serverless (upstash) profile / Vercel.
# Torch-free: Upstash embeds server-side, so no sentence-transformers/torch here.
# For the es/memory profiles (and the Dockerfile), use requirements-full.txt.

# -- Web / API --
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2

# -- LLM --
groq==0.37.1

# -- Serverless vector store (server-side hybrid embeddings) --
upstash-vector==0.8.0

# -- Document loading / chunking (light; no torch) --
pymupdf==1.28.0
python-docx==1.2.0
langchain-text-splitters==0.2.4
