# Askable backend — pinned for reproducible container builds.
#
# torch is installed CPU-only via the PyTorch CPU wheel index (see Dockerfile);
# the default index would pull the multi-GB CUDA build we don't need.

# -- Web / API --
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2

# -- LLM --
groq==0.37.1

# -- Retrieval / ML --
sentence-transformers==5.6.0
torch==2.12.1
numpy==1.26.4
rank-bm25==0.2.2

# -- Document loading / chunking --
pymupdf==1.28.0
python-docx==1.2.0
langchain-community==0.2.19
langchain-text-splitters==0.2.4

# -- Optional backends (imported only when BACKEND=elasticsearch) --
# Kept so the same image can run the ES+Redis profile if pointed at those services.
elasticsearch==9.4.1
redis==8.0.1
